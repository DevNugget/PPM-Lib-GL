import ../ppm_lib/ppm_toolkit
import ../ppm_lib/ppm_sprite

var sprite = createSprite(
    "face_sprite.sprite", "sprite.ppm", @[8, 6], 1, @[255, 255, 255]
)

createImage(sprite, "sprite.png")
